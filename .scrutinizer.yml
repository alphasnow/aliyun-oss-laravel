checks:
    php:
        code_rating: true
        duplication: true
filter:
    excluded_paths:
        - tests/*
build:
    tests:
        override:
            -
                command: ./vendor/bin/phpunit --coverage-clover=phpunit-coverage-file
                coverage:
                    file: phpunit-coverage-file
                    format: php-clover